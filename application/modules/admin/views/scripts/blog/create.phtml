<p>Complete the form to create a new blog to be aggregated by the Planet. The blog details
will be collected from the given feed URI.</p>
<? if(isset($this->messages)): ?>
    <p class="<?= $this->escape($this->messages[1]) ?> message"><?= $this->escape($this->messages[0]) ?><p>
<? endif; ?>
<?= $this->addBlogForm ?>
